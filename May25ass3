Class solution: 
   def plusOne(self,digits):
       for i in range(Len(digits)):
          if digits[~i]<9
              digits[~i]+=1
              return digits
            digits[~i]=0
          return [1] + [0]*Len( digits)
